interface:
  display_name: "Kilo Delegator"
  short_description: "Save Codex usage with worker-owned implementation"
  default_prompt: "Use $kilo-delegator to outsource suitable implementation, tests and corrections, then verify the result before integration."
policy:
  allow_implicit_invocation: true
